---
title: I/O 多路复用是什么
card: true
date: 2026-01-27 10:33:44
tags:
  - interview
  - bytedance
  - os
---

I/O 多路复用（I/O Multiplexing）是一种高性能的网络编程技术，它允许 **单个线程同时监控多个网络连接（文件描述符）** 的状态。当某个连接就绪（如数据可读或可写）时，内核会通知程序进行处理，从而避免了为每个连接创建独立线程带来的巨大资源损耗，也解决了非阻塞轮询导致的 CPU 浪费，是构建高并发服务器（如 Redis、Nginx）的核心基石。
